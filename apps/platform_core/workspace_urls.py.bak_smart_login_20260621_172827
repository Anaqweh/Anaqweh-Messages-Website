from django.urls import path
from . import workspace_views

app_name = "workspace"

urlpatterns = [
    path("", workspace_views.home, name="home"),
    path("<int:tenant_pk>/", workspace_views.dashboard, name="dashboard"),
]
